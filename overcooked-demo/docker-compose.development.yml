version : '3.7'

services:
    app:
        build:
            context: ./server
            args:
                BUILD_ENV: development
        environment: 
            FLASK_ENV: development
        ports:
            - "8080:5000"
        
